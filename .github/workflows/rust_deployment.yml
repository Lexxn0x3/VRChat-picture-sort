name: Deployment

concurrency: production

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  deployment:
    runs-on: ubuntu-lates

    steps:
    - uses: actions/checkout@v3
    - name: Build
      run: cargo build --verbose --release
